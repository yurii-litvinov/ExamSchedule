import {Layout} from "@shared/ui/layout/Layout.tsx";


/// Not found page
export function NotFoundPage() {
    return (
        <Layout>
            <h1>Page not found.</h1>
        </Layout>
    );
}